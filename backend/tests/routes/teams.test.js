import {jest} from '@jest/globals';

test('sample test', () => {
    expect(true).toBe(true)
})

// // Purpose: Returns an array of teams stored in the database
// router.get('/teams', getTeams)

// // Purpose: Adds team to database iff the conditions are met
// router.post('/teams', async (req, res) => {}